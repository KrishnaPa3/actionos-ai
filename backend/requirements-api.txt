# =============================================================================
# ActionOS API - direct dependencies only.
#
# Deliberately contains NO torch, whisper, whisperx, pyannote, lightning,
# transformers, numpy, scipy, scikit-learn or matplotlib. All audio/ML work
# now lives in the separate gpu-worker service and is reached over HTTP.
#
# Derived from an AST scan of every import in backend/, not from pip freeze,
# so pip resolves the transitive set itself.
# =============================================================================

# --- Web framework ---
fastapi==0.137.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4

# --- Config / HTTP ---
python-dotenv==1.2.2
httpx==0.28.1

# --- Database + auth ---
supabase==2.31.0

# --- LLM client (Ollama exposes an OpenAI-compatible API) ---
openai==2.43.0

# --- Due-date parsing for extracted tasks ---
dateparser==1.4.1

# --- Integrations ---
notion-client==3.1.0
slack_sdk==3.43.0
google-api-python-client==2.198.0
google-auth==2.56.2
google-auth-httplib2==0.4.0
